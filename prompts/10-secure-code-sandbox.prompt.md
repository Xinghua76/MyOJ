实现代码沙箱模块（安全隔离）：
- 采用 Docker 容器或本地沙箱执行用户代码；
- 限制 CPU 时间、内存、进程数、文件大小；禁用网络；只读文件系统（可选）；
- 判题结束销毁环境，清理临时文件；
- 检测危险行为（fork bomb、写系统目录、网络访问、异常系统调用）并返回“安全违规”。
请输出：
1) 具体隔离方案（Docker run 参数 / cgroups / seccomp / ulimit）以及理由。
2) 目录挂载策略（只挂载工作目录与测试数据）。
3) 多语言编译/运行命令模板（C/C++/Java/Python/JS 等按我项目支持的语言）。
4) 风险点与兜底（超时杀进程、容器泄露、日志脱敏）。
