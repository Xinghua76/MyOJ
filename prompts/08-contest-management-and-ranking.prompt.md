实现比赛模块：
- 列表：比赛列表（未开始/进行中/已结束）、详情、报名参赛。
- 比赛中：题目列表、提交、实时排行榜（至少支持定时刷新；可选 WebSocket）。
- 系统控制：比赛自动开启/关闭；结束后生成成绩单（可导出 CSV/PDF）。
关键点：
1) 比赛时间与提交有效性校验（比赛外提交是否计入）。
2) 排行榜规则：按 AC 数→罚时→最后 AC 时间（可配置）。
3) 生成成绩单的表结构与任务机制（定时任务/消息队列）。
请输出：数据模型、接口、排行更新策略（实时/准实时）、前端页面流程、异常边界。
